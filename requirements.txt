# Adaptive Payment Revenue Recovery Agent — Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Orchestration
langgraph>=0.1.0

# Vector store (local persistent directory mode — no separate Chroma server)
chromadb>=0.5.0

# Relational DB + ORM
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ML / embeddings
sentence-transformers>=3.0.0

# Scheduling (local, in-process — replaces EventBridge/SNS locally)
apscheduler>=3.10.4

# Testing
pytest>=8.2.0
httpx>=0.28.0